#include<bits/stdc++.h>

using namespace std;
int n,A,B,M;
vector<int> treeHash;
vector<vector<int>> children;
int main(){
    cin >> n;
    treeHash.resize(n+1);
    children.resize(n+1);

    for(int i = 2 ; i<= n ; ++i){
        
    }
    return 0;
}